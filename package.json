{
  "name": "multiplayer-github",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "build": "tsc --noEmit",
    "build:extension": "node esbuild.mjs"
  },
  "dependencies": {
    "partysocket": "0.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.19.4",
    "partykit": "0.0.114",
    "typescript": "^5.2.2"
  }
}
